This code is a basic example of autotest with python Selenium using "Page Object Model" pattern. The task is simple - open a prepared link, put a book in a bucket, confirm it's in the bucket, close browser.

